# Scripts système

## Arborescence

* `bin/` : exécutables
* `etc/` : configurations
* `lib/` : librairies
* `src/` : sources

## Installation

```bash
# installation
make

# alternativement
make install

# désinstallation
make uninstall
```
