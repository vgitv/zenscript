#!/bin/bash
set -euo pipefail

# Sript for testing library importation.

scriptPath="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
libPath=${scriptPath}/../lib
source ${libPath}/misc.sh

title 'test'
